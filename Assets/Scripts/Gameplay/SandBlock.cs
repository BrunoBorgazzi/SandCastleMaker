using UnityEngine;

public class SandBlock : MonoBehaviour
{
    // Este script sirve como "etiqueta" para que la Kill Zone reconozca que lo que cayó es arena
    // y no otro objeto. Más adelante podemos meterle lógica de si es arena húmeda o seca.
}
